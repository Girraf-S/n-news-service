spring:
  application:
    name: n-news-service
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    open-in-view: false

  datasource:
    url: jdbc:postgresql://localhost:5432/n-news-service
    username: postgres
    password: mitrandir

jwt:
  key: Nikon1234
  bearer: 'Bearer '
  begin-index: 7

server:
  port: 8082
